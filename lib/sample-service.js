'use strict';

module.exports = exports = function(container, config) {

  container.factory([
    // Required dependencies
    'sample/service'
  ], function(
    // Instantiated dependencies
    sample
  ) {

    // Construct service instance

    // Return promise or instance

  });

};
